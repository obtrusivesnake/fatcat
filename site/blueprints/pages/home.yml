title: Homepage

status: false

options:
  changeTemplate: false
  changeStatus: false

tabs:
  content:
    label: Content
    icon: text
    fields:
      blocks:
        type: blocks
        label: Page Sections
        empty: Add a section to build the homepage.
        fieldsets:
          - hero
          - trust_strip
          - left_image
          - right_image
          - class_list
          - fitness_list
          - calendar

  seo:
    label: SEO
    icon: search
    fields:
      meta_description:
        label: Meta Description
        type: textarea
        size: small
        maxlength: 160
        help: A short summary (up to 160 characters) shown in search engine results.
